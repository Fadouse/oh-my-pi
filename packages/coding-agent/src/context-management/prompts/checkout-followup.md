`context_checkout` complete. The branch summary above replaces your previous turn's history. Before any other action, restate (a) the current Objective, (b) preserved User Constraints, and (c) the exact Next Step. If any are missing, run `context_log` and consider checking out the backup tag with `mode: "recover"`. Do not rely on details from before the checkout unless they were preserved in the summary.
