`context_checkout` applied. Continue from the branch summary now in context; it is the source of truth. First verify Objective, User Constraints, Current Artifact, and Next Step are present. If any are missing, recover with the backup tag using `mode:"recover"`; otherwise continue from the summary without re-reading old history.
