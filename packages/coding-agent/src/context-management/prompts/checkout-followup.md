`context_checkout` complete. The branch summary above is now your source of truth. Before any other action, extract Objective, User Constraints, Current Artifact, and Next Step from it. If any are missing or too vague, you MUST NOT continue implementation; use `context_log`/`context_search` or recover the backup tag with `mode: "recover"`. Continue only from a complete handoff.
